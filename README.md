# blink_arduino_bluetooth_connection
Se enciende y se apaga el led de la placa Arduino UNO (correspondiente al pin 13), mediante el envío de comandos (unos y ceros) y atraves de una conexion bluetooth.
